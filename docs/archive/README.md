# Archived Documentation Material

This directory stores non-canonical support material that should remain available without competing with the active documentation tree.

- `reference_material/` keeps office or exported files preserved for historical context.

If a file is required for the current operational or methodological path, it should live under `docs/architecture/`, `docs/operations/`, or `docs/thesis/` instead of here.