# Core
numpy>=1.24
pandas>=2.0
psutil>=5.9

# Deep learning (choose one of the install routes below)
# 1) Conda (recommended): use environment.yml instead of this file
# 2) Pip + prebuilt wheels (ensure CUDA toolkits/drivers match):
#    See https://pytorch.org/get-started/locally/ for the exact command.
#    For example (CUDA 12.1):
#    pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
#    Or CPU-only:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Transformers/NLP
transformers>=4.40
huggingface_hub>=0.20
safetensors>=0.4

# GPU telemetry
pynvml>=11.5

# CPU energy (requires permissions on intel-rapl sysfs)
pyRAPL>=0.2.3
